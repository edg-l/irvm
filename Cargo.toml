[package]
name = "irvm"
version = "0.1.0"
edition = "2024"
description = "A IR compiler target with a Rust friendly API that lowers to LLVM IR."
keywords = ["llvm", "compiler", "ir", "compiler-target", "llvm-ir"]
categories = ["compilers"]
license = "Apache-2.0"

[dependencies]
